{
  "name": "lights-on",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "cd server && npm install --omit=dev",
    "start": "node server/index.js"
  },
  "engines": {
    "node": ">=20"
  }
}
